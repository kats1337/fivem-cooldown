# Cooldown system for FiveM server.
Everything should be documented that you'd need to know.

### Triggers "resName:cooldownExpired" after the cooldown has expired. Also sends the given identifier as only param

